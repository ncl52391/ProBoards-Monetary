Monetary System 0.9.7
=====================

Forum Topic - http://support.proboards.com/thread/429762/

Build
-----

Rebuild the ProBoards import bundle after editing source JavaScript:

```
python scripts/build_pbp.py
```

The builder preserves the existing `.pbp` metadata and replaces the main JavaScript component from the source files.